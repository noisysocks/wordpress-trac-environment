# WordPress Trac Environment

To start the environment:

```
git submodule init
git submodule update
./env.sh start
```

Trac will then be running at <http://localhost:8123/>.

To stop the environment:

```
./env.sh stop
```
